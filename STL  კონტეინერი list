კლასი list ჰქმნის ორმხრივ ბმულ სიას. ვექტორისაგან განსხვავებით, რომელიც უზრუნველყოფს თავისუფალ წვდომას თავის ელემენტებზე, 
სიის ელემენტებისადმი წვდომა მხოლოდ მიმდევრობითია. მოძრაობა შესაძლებელია სიის დასაწყისიდან ბოლოსაკენ და პირიქით.

#include <bits/stdc++.h>
using namespace std;
int main (){
  int arr[] = {25,53,16,27,22,73,61};
  list<int> lst (arr,arr+7);
  list<int>::iterator it;
 
  for ( it=lst.begin(); it != lst.end(); ++it){
    cout << *it<<" ";
  }
}
